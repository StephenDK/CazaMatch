const AddPropertyPage = () => {
  return <div>Add Property Page</div>;
};

export default AddPropertyPage;

// This is an example of a nested route.
// This page loads when a user visits "properties/add"
